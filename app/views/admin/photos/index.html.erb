<%= link_to('上傳菜單', new_admin_photo_path) %><p>
<% @photos.each do |i| %>
	<%= i.name %>
	<%= image_tag i.image_url(:thumb).to_s %><p>
	<%= link_to('編輯', edit_admin_photo_path(i)) %>
	<%= link_to('刪除', admin_photo_path(i), :method => 'delete') %><p>
<% end %>
